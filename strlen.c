#include "shell.h"

/**
 * str_len - checks the length of a string
 * @string:string
 *
 * Return:returns len
 */
int str_len(const char *string)
{
	int i = 0;

	for (; string[i] != '\0'; i++)
	{
		;
	}
	return (i);
}
